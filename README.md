# noctalia-client Plugin

Listen your music hosted on a subsonic compatible server. Provides action for the Noctalia launcher.

## Requirements

- `mpv` for playback
- `mpv-mpris` for playback control
- `socat` to control mpv
